boolean genetics = true;
boolean clonacion = false;

// or
if (genetics || clonacion) {
  print("operador or");
}

// and
if (genetics && clonacion) {
  print("operador or");
}

// not
if (!clonacion) {
  print("operador or");
}
